/** Add-on fee per child when “Return gifts” is selected (edit for your venue). */
export const BIRTHDAY_RETURN_GIFT_PER_CHILD_INR = 199;

export const BIRTHDAY_KID_MIN = 1;
export const BIRTHDAY_KID_MAX = 50;

/** Preferred party date must fall within this many days from today (if provided). */
export const BIRTHDAY_PREFERRED_MAX_DAYS_AHEAD = 365;
